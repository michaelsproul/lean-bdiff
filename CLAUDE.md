- Use the available MCP for lean-lsp to avoid rebuilding the project from scratch when working on proofs.
- Never write `partial` functions, they cannot be reasoned about formally.
- Do not use `unsafe`.
- Do not use `implemented_by`.

